# 核心安全层（L0/L1/L3/L4 + 协调器）零第三方依赖，纯标准库即可运行与测试。
# 以下依赖仅生产部署（真实模型/向量库/HTTP 服务）时需要。

# --- HTTP 服务 ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- L2 推理（真实后端，二选一或都装）---
openai>=1.30                 # 走 vLLM 的 OpenAI 兼容端点
chromadb>=0.5                # 向量库
sentence-transformers>=2.7   # embedding

# --- 配置/测试 ---
PyYAML>=6.0
pytest>=8.0

# 注：vLLM 需单独按 GPU 环境安装（pip install vllm），不在本文件强制约束。
